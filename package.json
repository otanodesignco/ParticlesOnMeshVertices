{
  "dependencies": {
    "three": "^0.141.0"
  },
  "scripts": {
    "start": "parcel serve index.html --port 8080 --open",
    "build": "rm -rf ./dist; parcel build index.html --public-url '.' --no-source-maps"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.2",
    "parcel-bundler": "1.12.3",
    "parcel-plugin-static-files-copy": "^2.6.0"
  }
}
